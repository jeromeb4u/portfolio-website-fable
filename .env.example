# Copy to .env and fill in. See plan/02-tech-stack.md.
# Neon Postgres connection string. Leave unset for local dev (SQLite fallback).
DATABASE_URI=
# openssl rand -hex 32
PAYLOAD_SECRET=
NEXT_PUBLIC_SITE_URL=http://localhost:3000
RESEND_API_KEY=
CONTACT_TO_EMAIL=jeromeb4u@gmail.com
# Vercel Blob media storage (production)
BLOB_READ_WRITE_TOKEN=
